#!/bin/sh
# 아무것도 하지 않는다.
#
# hook 실험은 feat/hook-discovery 브랜치에 있다. 그 브랜치에서 main으로 옮기면
# 파일이 사라지는데, 이미 떠 있는 세션은 설정을 세션 시작 때 읽으므로 계속
# 이 경로를 부른다. 파일이 없으면 매 턴 에러가 난다. 그 세션들이 끝날 때까지
# 조용히 받아 넘긴다.
exec cat > /dev/null 2>&1 || true
